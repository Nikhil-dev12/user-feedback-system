import { CapitalizeNamePipePipe } from './capitalize-name-pipe.pipe';

describe('CapitalizeNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
